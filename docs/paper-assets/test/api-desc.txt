① 教师发布课程接口：POST /api/course/add 返回 200 + 发布成功
② 查看选课学生接口：GET /api/course/{id}/students 返回 List<User>
③ 管理员用户列表接口：GET /api/user/list 返回分页 IPage<User>
④ 登录接口：POST /api/login 返回 JWT token（HS256 32 字节密钥）
